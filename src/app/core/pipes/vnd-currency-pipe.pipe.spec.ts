import { VndCurrencyPipePipe } from './vnd-currency-pipe.pipe';

describe('VndCurrencyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VndCurrencyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
